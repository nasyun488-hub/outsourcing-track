# 外协工序流转追踪系统 — 业务规则梳理 v1

> 作者：好妹 | 日期：2026-05-23
> 目的：彻底厘清实际业务工序流转的各种场景、规则及数据逻辑联动关系，
>       为系统测试提供完整的场景矩阵和验证依据。

---

## 一、系统实体与数据模型

### 1.1 核心实体

| 实体 | 说明 | 关键字段 |
|------|------|----------|
| Factory | 工厂（primary=主厂，cooperative=外协厂） | factory_id, factory_type |
| Process | 工序定义（属于某订单的某道序） | process_id, order_id, process_order, process_name |
| ProcessRecord | 工序流转记录（核心！） | record_id, process_id, factory_id, record_status, lock_type |
| ReceiveBatch | 接收批次（一次接收算一批） | batch_id, record_id, receive_qty, receive_time |
| ShipBatch | 发出批次 | batch_id, record_id, ship_qty, ship_time |
| ReturnRecord | 退件记录 | return_id, from/to_record_id, return_qty |
| Order | 订单 | order_id, primary_factory_id, order_status |
| Notification | 通知 | notif_id, user_id, notif_type, is_read |

### 1.2 工序记录核心字段（ProcessRecord）

```
record_id         唯一标识（如 R0101）
order_id          所属订单（如 O01）
process_id        工序定义ID（如 P0101）
factory_id        负责厂家（F001/F002/F003）
record_status     工序状态（pending/received/shipped/completed）
lock_type         锁定类型（none/entry_lock/relation_lock/sync_lock）
total_receive_qty 累计接收数量
total_ship_qty    累计发出数量
partial_receive   是否分批接收（0/1）
partial_ship      是否分批发货（0/1）
last_receive_time 最后接收时间
last_ship_time    最后发出时间
created_at        记录创建时间
```

### 1.3 锁定类型（lock_type）含义

| lock_type | 含义 | 可进行的操作 |
|------------|------|-------------|
| `none` | 无锁定 | 可接收、可发出 |
| `entry_lock` | 入口锁定：已接收，等待本厂处理完成 | 可发出（解锁后）|
| `relation_lock` | 关联锁定：下道已接收，本道不可单独操作 | 不可操作 |
| `sync_lock` | 同步锁定：已同步至MOM系统 | 不可操作 |

---

## 二、工序状态机

### 2.1 工序状态（record_status）

```
pending → received → shipped → completed
```

| 状态 | 含义 | 触发条件 |
|------|------|----------|
| `pending` | 待接收 | 订单创建时默认状态 |
| `received` | 已接收 | 首次录入接收数量 |
| `shipped` | 已发出 | 首次录入发出数量 |
| `completed` | 已完成 | 整个订单完成（需确认逻辑） |

### 2.2 状态转换规则

#### 规则1：接收（receive）
- **触发**：操作员在 H5 扫码录入"接收"数量
- **前置条件**：
  - 上道工序必须有 `total_ship_qty > 0`（第一道工序无此限制）
  - 本道 `lock_type == 'none'`（无锁定）
  - 用户属于本道 `factory_id` 对应工厂
- **效果**：
  - 创建 `ReceiveBatch` 记录
  - `total_receive_qty += receive_qty`
  - `last_receive_time = receive_time`
  - 若原本 `record_status == 'pending'`：→变为 `received`，`lock_type → 'entry_lock'`
  - 若原本 `record_status == 'received'`：保持 received（分批接收）

#### 规则2：发出（ship）
- **触发**：操作员在 H5 扫码录入"发出"数量
- **前置条件**：
  - `total_receive_qty >= ship_qty`（有货可发）
  - `lock_type == 'none'`（不能是 entry_lock/relation_lock/sync_lock）
  - 用户属于本道 `factory_id` 对应工厂
- **效果**：
  - 创建 `ShipBatch` 记录
  - `total_ship_qty += ship_qty`
  - `last_ship_time = ship_time`
  - `record_status → 'shipped'`
  - 若本道有 `relation_lock` 的上道 → 自动升级为 `relation_lock`（上下道联动）

#### 规则3：退件（return）
- **触发**：在"发出"页面发起退件
- **前置条件**：
  - `return_qty > 0`
  - 退件原因必填
  - 双方 `lock_type` 均不能是 `relation_lock/sync_lock`
  - 退件量 ≤ 接收方已接收量
- **效果**：
  - 创建 `ReturnRecord`
  - 发送方：`total_ship_qty -= return_qty`，`partial_ship = 1`
  - 接收方：`total_receive_qty -= return_qty`，`partial_receive = 1`
  - **不退状态**：即使减到0，双方 record_status 不回退

#### 规则4：relation_lock 升级
- **触发**：下道工序被接收
- **效果**：上道 `lock_type → 'relation_lock'`
- **含义**：下道已确认收货，上道不可再操作

#### 规则5：sync_lock 升级
- **触发**：数据同步至 MOM 系统后
- **效果**：`lock_type → 'sync_lock'`
- **含义**：已与 MOM 同步，不可单方面修改

---

## 三、数量联动规则

### 3.1 核心数量关系

```
订单总量（Order.total_qty）
  = Σ 各工序 total_receive_qty（最终应等于订单总量）
  = Σ 各工序 total_ship_qty（流转过程中）

对于单道工序：
  total_receive_qty ≤ Σ 上道 total_ship_qty（上道没发那么多，本道收不了）
  total_ship_qty ≤ total_receive_qty（发的不能超过收的）
```

### 3.2 分批机制

- **分批接收**：`partial_receive = 1`（当 batch_no > 1）
- **分批发货**：`partial_ship = 1`（当 batch_no > 1）
- 批次号 = 该工序已有批次数量 + 1

### 3.3 超期判定

> 超期阈值：48小时

**pending 超期**：订单创建后（`created_at`）超过 48 小时，仍为 `pending` 状态（未接收）

**received 超期**：已接收（`last_receive_time` 有值）超过 48 小时，仍为 `received` 状态（未发出）

> ⚠️ 注意：超期只看当前状态，不看 `lock_type`。

---

## 四、订单状态（order_status）

| 状态 | 含义 | 触发条件 |
|------|------|----------|
| `pending` | 待处理 | 订单刚创建 |
| `in_progress` | 进行中 | 任意工序开始流转 |
| `completed` | 已完成 | 全部工序 `completed`？ |

> ⚠️ 当前代码中订单状态与工序状态之间的联动**未验证**（可能脱钩）

---

## 五、扫码判断逻辑（scan_judge）

二维码格式：`record_{record_id}` 或 `process_{process_id}_{factory_id}`

| 工序状态 | 锁定类型 | 跳转页面 |
|----------|----------|----------|
| `pending` | any | receive（接收页）|
| `received` | any | ship（发出页）|
| `shipped` | any | view（查看页）|
| `completed` | any | view（查看页）|

---

## 六、权限体系

### 6.1 角色

| 角色 | 说明 | 可操作工厂 |
|------|------|-----------|
| `enterprise_admin` | 企业管理员 | 全部 |
| `primary_admin` | 主厂管理员 | F001 |
| `primary_operator` | 主厂操作员 | F001 |
| `cooperative_admin` | 外协厂管理员 | F002/F003 |
| `cooperative_operator` | 外协厂操作员 | F002/F003 |

### 6.2 权限检查

操作记录时：`user.factory_id == record.factory_id`

> ⚠️ 但 `get_current_user` 的 `check_permission` **未检查用户工厂与工序工厂的匹配**。
> 只要有操作员角色即可操作任意工厂的记录（权限漏洞）。

---

## 七、通知触发场景（推测）

从测试数据中的5条通知逆推触发场景：

| 通知 | 类型 | 触发场景推测 |
|------|------|-------------|
| N001 超期预警 | transfer | pending 超48h 自动触发 |
| N002 待处理 | transfer | 工序等待某厂接收（新建工序）|
| N003 订单完成 | transfer | 订单全部工序 completed |
| N004 退件通知 | transfer | 退件操作成功后触发 |
| N005 新工序 | transfer | 新建工序记录（来自MOM同步）|

---

## 八、当前数据状态（20订单36工序）

```
O01: P0101(received@F002, 100件) → P0102(pending@F003) → P0103(pending@F001)
O02: P0201(shipped@F002, 200件) → P0202(received@F003, 200件)  ← 断链！
O03: P0301(received@F002, 150件) → P0302(received@F001, 150件)
O04: P0401(pending@F002) ← 96h超期！
O05: P0501(pending@F002) ← 72h超期！
O06: P0601(received@F002) ← 55h超期！
O07: P0701(received@F003, 110件)
O08: P0801(received@F002) → P0802(pending@F003) → P0803(pending@F001)
O09: P0901(shipped@F002) → P0902(received@F003) → P0903(pending@F002)  ← 断链！
O10: P1001(received@F002, 100收/80发) → P1002(pending@F003)  ← 部分发货
O11: P1101(shipped@F002) → P1102(pending@F003)
O12: P1201(completed@F002) → P1202(completed@F003)  ← 正常完成
O13: P1301(completed@F002) → P1302(completed@F003)  ← 正常完成
O14: P1401(completed@F002)  ← 单工序完成
O15: P1501(pending@F002)
O16: P1601(pending@F002)
O17: P1701(pending@F002) ← 52h超期！
O18: P1801(received@F002)
O19: P1901(completed@F002) → P1902(received@F003) → P1903(pending@F001)  ← 断链！
O20: P2001(received@F002) → P2002(received@F003) → P2003(pending@F001)  ← 断链！
```

---

## 九、验证不充分的问题清单

### 问题1：O02订单"断链"——P0202 received但上道P0201 shipped状态正常
- **现象**：O02的P0201(shipped, 200发) → P0202(received, 200收)，看起来正常
- **关注点**：P0202的lock_type应该是none（因为下道工序还没被接收），但record_status=received说明它已接收了货物

### 问题2：O09/O19/O20"断链"——P0903/P1903/P2003 pending，但上道已发出
- **现象**：O09的P0902(received)但P0903(pending)；O19的P1902(received)但P1903(pending)；O20的P2002(received)但P2003(pending)
- **业务含义**：这些pending工序是第一道吗？不是（P0903=第3道）。为何pending？可能是下游工序还未完成/发出

### 问题3：O10部分发货——total_receive=100, total_ship=80
- **关注点**：部分发货后record_status仍为received（因为没改回pending），lock_type？

### 问题4：订单状态（order_status）更新机制不明
- **关注点**：O02明明有shipped工序了，但order_status=pending，未变为in_progress
- **待验证**：order_status何时更新？由谁更新？

### 问题5：lock_type与record_status的联动
- **关注点**：received状态一定对应entry_lock吗？completed状态对应什么lock_type？
- **当前数据**：R1201、R1301、R1401、R1901都是completed但lock_type是什么？

---

## 十、测试场景矩阵（应覆盖）

### 场景A：正常工序流转
| 步骤 | 操作 | 前置条件 | 预期结果 |
|------|------|----------|----------|
| A1 | 接收（第一道）| 上道无 | status→received, lock→entry_lock |
| A2 | 发出 | 已接收，lock=none | status→shipped, 上道→relation_lock |
| A3 | 下道接收 | 上道已发出 | 下道status→received, lock→entry_lock，上道→relation_lock |

### 场景B：分批操作
| 步骤 | 操作 | 前置条件 | 预期结果 |
|------|------|----------|----------|
| B1 | 分批接收 | pending状态 | 累加receive_qty，partial_receive=1 |
| B2 | 分批发货 | 已接收 | 累加ship_qty，partial_ship=1 |

### 场景C：退件
| 步骤 | 操作 | 前置条件 | 预期结果 |
|------|------|----------|----------|
| C1 | 退件 | 退件量≤已收量，双方unlocked | 双方qty减少，partial标志=1 |
| C2 | 退件后重发 | 上道解锁可再次发货 | 数量重新累加 |

### 场景D：超期
| 步骤 | 状态 | 时长 | 预期结果 |
|------|------|------|----------|
| D1 | pending | >48h | is_overdue=True |
| D2 | received | >48h未发出 | is_overdue=True |
| D3 | pending | <48h | is_overdue=False |

### 场景E：锁定状态
| 步骤 | 操作 | 前置lock_type | 预期结果 |
|------|------|---------------|----------|
| E1 | 接收 | none | 成功 |
| E2 | 发出 | none | 成功，status→shipped |
| E3 | 发出 | entry_lock | 拒绝 |
| E4 | 接收 | entry_lock | 拒绝 |
| E5 | relation_lock | 收到下道接收通知 | 自动升级 |

### 场景F：权限
| 步骤 | 用户 | 目标工序 | 预期结果 |
|------|------|----------|----------|
| F1 | U002(F002) 操作F002工序 | F002 | 允许 |
| F2 | U002(F002) 操作F003工序 | F003 | 拒绝 |
| F3 | enterprise_admin | 任意 | 允许 |

### 场景G：数量校验
| 步骤 | 操作 | 数量 | 预期结果 |
|------|------|------|----------|
| G1 | 接收 | >上道发出量 | 拒绝 |
| G2 | 发出 | >已接收量 | 拒绝 |
| G3 | 退件 | >接收方已收量 | 拒绝 |
