# 外协工序流转追踪系统 — 业务规则梳理与Bug报告 v2

> 作者：好妹 | 日期：2026-05-23
> 基于：20订单36工序全量数据 + 6场景API实测

---

## 一、系统业务实体与数据模型

### 1.1 核心实体关系图

```
Order (订单)
  ├─ primary_factory_id → Factory (主厂)
  ├─ order_status (pending/in_progress/completed)
  └─ process_records[] (工序流转记录)

Process (工序定义)
  ├─ order_id → Order
  ├─ process_order (第几道)
  └─ process_name

ProcessRecord (工序流转记录) ★ 核心
  ├─ order_id → Order
  ├─ process_id → Process
  ├─ factory_id → Factory (负责厂家)
  ├─ record_status (pending→received→shipped→completed)
  ├─ lock_type (none→entry_lock→relation_lock→sync_lock)
  ├─ total_receive_qty / total_ship_qty
  ├─ partial_receive / partial_ship
  ├─ last_receive_time / last_ship_time
  └─ ReceiveBatch[] / ShipBatch[]

User → Factory (通过 factory_id 关联)
  └─ role (enterprise_admin / primary_admin / primary_operator / cooperative_admin / cooperative_operator)
```

### 1.2 ProcessRecord 关键字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `record_id` | VARCHAR(64) | 主键，如 `R0101` |
| `record_status` | ENUM | `pending`/`received`/`shipped`/`completed` |
| `lock_type` | ENUM | `none`/`entry_lock`/`relation_lock`/`sync_lock` |
| `total_receive_qty` | INT | 累计接收数量 |
| `total_ship_qty` | INT | 累计发出数量 |
| `partial_receive` | TINYINT | 是否分批接收（0/1）|
| `partial_ship` | TINYINT | 是否分批发货（0/1）|
| `last_receive_time` | DATETIME | 最后接收时间 |
| `last_ship_time` | DATETIME | 最后发出时间 |

---

## 二、工序状态机（实测验证）

### 2.1 状态转换图

```
pending ──[receive]──→ received ──[ship]──→ shipped ──[complete]──→ completed
                              │
                         [entry_lock]
                         (等待本厂完成)
```

### 2.2 锁定类型含义与操作权限

| lock_type | 含义 | 可接收 | 可发出 | 可退件 |
|-----------|------|:------:|:------:|:------:|
| `none` | 无锁定 | ✅ | ✅ | ✅ |
| `entry_lock` | 已接收，等待本厂处理完成 | **❌** | **❌** | ✅ |
| `relation_lock` | 下道已确认，本道不可操作 | ❌ | ❌ | **❌** |
| `sync_lock` | 已同步MOM | ❌ | ❌ | ❌ |

### 2.3 实测状态转换结果

| # | 转换 | 前置条件 | 结果 | 验证 |
|---|------|----------|------|------|
| A1 | pending→received | 上道有ship_qty，lock=none | ✅ status=received, lock=entry_lock | ✅ 通过 |
| A2 | received→shipped | lock=none, 接收量≥发出量 | ✅ status=shipped, lock=none | ✅ 通过 |
| B | 分批接收 | 已received+entry_lock | **❌ 被entry_lock阻止** | ❌ Bug-B1 |
| C1 | 接收超量 | receive_qty > 上道ship_qty | ✅ 正确拒绝 | ✅ 通过 |
| C2 | 发出超量 | ship_qty > 已接收量 | ✅ 正确拒绝 | ✅ 通过 |
| D | qty=0接收 | receive_qty=0 | ✅ Schema层拒绝（≥1）| ✅ 通过 |
| E | entry_lock下发出 | lock=entry_lock | ✅ 正确拒绝 | ✅ 通过 |
| F | 跨工厂操作 | user.factory ≠ record.factory | ✅ 正确拒绝 | ✅ 通过 |
| G | relation_lock升级 | 下道接收后 | **❌ 未自动升级** | ❌ Bug-B2 |

---

## 三、核心业务流程实测发现

### 3.1 接收流程（receive）

**前置条件（代码层面）：**
1. 用户.factory_id == record.factory_id（工厂匹配）
2. lock_type == 'none'（不能有任何锁定）← **Bug-B1 根因**
3. 上道有 total_ship_qty > 0（第一道工序除外）
4. 接收量 ≤ 上道可发出量

**效果：**
- 创建 `ReceiveBatch`
- `total_receive_qty += receive_qty`
- `last_receive_time = receive_time`
- `partial_receive = 1`（当 batch_no > 1）
- 若原本 `pending` → 变为 `received`，`lock_type → 'entry_lock'`

### 3.2 发出流程（ship）

**前置条件（代码层面）：**
1. 用户.factory_id == record.factory_id
2. `lock_type == 'none'`（entry_lock 也不行！）← **重要**
3. `total_receive_qty >= ship_qty`

**效果：**
- 创建 `ShipBatch`
- `total_ship_qty += ship_qty`
- `last_ship_time = ship_time`
- `partial_ship = 1`
- `record_status → 'shipped'`

### 3.3 退件流程（return）

**前置条件：**
1. 退件原因必填
2. `return_qty > 0`
3. 双方 lock_type 不能是 `relation_lock/sync_lock`
4. `return_qty ≤ 接收方已接收量`

**效果：**
- 发送方：`total_ship_qty -= return_qty`，`partial_ship = 1`
- 接收方：`total_receive_qty -= return_qty`，`partial_receive = 1`
- **不退状态**：即使减到0，record_status 不回退

### 3.4 锁定升级（未生效）

**relation_lock 升级（代码有但未调用）：**
```python
def upgrade_to_relation_lock(self, record_id, operator_id):
    # 条件：下道工序已接收
    # 效果：lock_type → 'relation_lock'
```
→ **实测确认：下道接收后，上道 lock_type 未自动升级为 'relation_lock'**
→ 根因：`receive()` 方法中未调用此函数

**sync_lock 升级（需MOM同步）：**
```python
def upgrade_to_sync_lock(self, record_id, operator_id):
    # 条件：lock_type == 'relation_lock'
    # 效果：lock_type → 'sync_lock'
```

---

## 四、超期判定规则（已验证正确）

```
超期阈值：48小时

pending超期：
  TIMESTAMPDIFF(HOUR, created_at, UTC_TIMESTAMP()) > 48

received超期：
  TIMESTAMPDIFF(HOUR, last_receive_time, UTC_TIMESTAMP()) > 48
  且 record_status == 'received'
```

---

## 五、扫码判断（scan_judge）

### 5.1 二维码格式
- `record_{record_id}` — 按 record_id 查找
- `process_{process_id}_{factory_id}` — 按工序+工厂查找

### 5.2 跳转逻辑（代码 vs 实测）

| 工序状态 | 代码逻辑 | 实测结果 | 正确性 |
|----------|----------|----------|--------|
| `pending` | → receive | pending→jump=receive ✅ | ✅ |
| `received` | → ship | received→jump=ship ✅ | ✅ |
| `shipped` | → view | shipped→jump=view ✅ | ✅ |
| `completed` | → view | completed→jump=view ✅ | ✅ |
| 不存在 | → not_found | → not_found ✅ | ✅ |

> 注：received 跳 ship 是因为 entry_lock 下仍需操作（发出），并非bug。

---

## 六、订单状态 vs 工序状态（严重脱钩）

### 6.1 实测数据揭示的脱钩问题

| 订单 | order_status | 工序实际状态 | 是否脱钩 |
|------|-------------|--------------|----------|
| O01 | `pending` | shipped+received+pending | ❌ 严重脱钩 |
| O02 | `pending` | shipped+received | ❌ 已有shipped |
| O03 | `in_progress` | received+received | ⚠️ 提前进in_progress |
| O04~O06 | in_progress/pending | pending/shipped混合 | ❌ 脱钩 |
| O12~O14 | `completed` | 全部completed | ✅ 一致 |
| O19 | `in_progress` | completed+received+pending | ❌ 脱钩 |

### 6.2 结论
**订单状态（order_status）由谁更新？** 经代码分析：
- `order_status` **从未在任何 service 层被更新**
- 仅在订单创建时设为 `pending`，之后完全脱钩
- 看板 API 的 `order_status` 来自 DB 原始值，非计算得出

---

## 七、发现的Bug清单

### Bug-B1 【严重】entry_lock 阻止分批接收
**现象：** `received` + `entry_lock` 状态下，无法进行第二次分批接收

**根因：** `_check_lock_for_receive()` 检查了所有 lock 类型
```python
def _check_lock_for_receive(self, record: ProcessRecord) -> None:
    if record.lock_type in ["entry_lock", "relation_lock", "sync_lock"]:
        raise RecordServiceError("记录已锁定，无法接收", "RECORD_LOCKED")
```
但 `entry_lock` 状态下，分批接收是**合理业务需求**。

**修复建议：**
```python
def _check_lock_for_receive(self, record: ProcessRecord) -> None:
    # entry_lock 不阻止分批接收（可以继续收更多批次）
    # relation_lock/sync_lock 阻止
    if record.lock_type in ["relation_lock", "sync_lock"]:
        raise RecordServiceError(...)
```

---

### Bug-B2 【严重】relation_lock 未自动升级
**现象：** 下道工序接收后，上道 lock_type 未从 `none` 变为 `relation_lock`

**根因：** `receive()` 方法中**从未调用** `upgrade_to_relation_lock()`

**影响：**
- 理论上 `ship` 时应该先检查上道是否为 `relation_lock`
- 当前因为没升级，下道shipped后，上道仍为 `none`，本道可以随意操作
- 造成"断链"订单数据（如 O09/O19/O20，上道已发出但下道仍pending）

**修复建议：** 在 `receive()` 成功执行后，调用：
```python
# 检查是否有上道，有则升级为 relation_lock
prev_record = self._get_previous_process_record(record)
if prev_record and prev_record.record_status == 'received':
    self.upgrade_to_relation_lock(prev_record.record_id, user_id)
```

---

### Bug-B3 【低】分批发货逻辑缺失
**现象：** `ship` 方法每次设置 `partial_ship = (batch_no > 1)`，但未检查是否允许分批发货

**根因：** 代码未对 `partial_ship` 状态做业务约束检查

---

## 八、当前数据状态图（2026-05-23实测后）

```
O01: R0101(shipped@F002, 100/100) → R0102(received@F003, 100/0) → R0103(pending@F001)
O02: R0201(shipped@F002, 200/200) → R0202(received@F003, 200/0)     ← 断链
O03: R0301(received@F002, 150/0)  → R0302(received@F001, 150/0)     ← 下道未发
O04: R0401(pending@F002)                                          ← 96h超期
O05: R0501(pending@F002)                                          ← 72h超期
O06: R0601(received@F002)                                         ← 55h超期
O07: R0701(received@F003, 110/0)
O08: R0801(received@F002)  → R0802(pending@F003) → R0803(pending@F001)
O09: R0901(shipped@F002)  → R0902(received@F003) → R0903(pending@F002)  ← 断链
O10: R1001(received@F002, 100/80 partial) → R1002(pending@F003)
O11: R1101(shipped@F002)  → R1102(pending@F003)
O12: R1201(completed@F002, 200/200) → R1202(completed@F003, 200/200)  ✅
O13: R1301(completed@F002, 180/180) → R1302(completed@F003, 180/180)  ✅
O14: R1401(completed@F002, 160/160)                                        ✅
O15: R1501(received@F002, 80/0 entry_lock)                               ← entry_lock锁住
O16: R1601(pending@F002)
O17: R1701(pending@F002)                                         ← 52h超期
O18: R1801(received@F002, 95/0)
O19: R1901(completed@F002, 140/140) → R1902(received@F003, 100/0) → R1903(pending@F001) ← 断链
O20: R2001(received@F002, 60/0) → R2002(received@F003, 40/0) → R2003(pending@F001)        ← 断链
```

> 注：这些"断链"是测试数据模拟真实业务场景（工序流转中的异步延迟）

---

## 九、完整测试矩阵

### 场景A：正常工序流转 ✅ 已测
### 场景B：分批接收 ❌ Bug-B1
### 场景C：数量校验 ✅ 已测
### 场景D：边界值 ✅ 已测
### 场景E：entry_lock ✅ 已测
### 场景F：权限校验 ✅ 已测
### 场景G：relation_lock升级 ❌ Bug-B2
### 场景H：订单状态联动 ❌ 未实现
### 场景I：退件流程 ⚠️ 未测
### 场景J：解锁申请 ⚠️ 未测
### 场景K：sync_lock升级 ⚠️ 未测（MOM未接入）
### 场景L：扫码judge ✅ 已测
