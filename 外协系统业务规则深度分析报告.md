# 外协工序流转追踪系统 — 业务规则深度分析报告

> 作者：好妹 | 日期：2026-05-23
> 基于：20订单36工序 + record_service.py 全量代码（624行）+ 实测API验证

---

## 一、核心实体关系与数据模型

### 1.1 实体ER关系

```
Order ──(1:n)── Process ──(1:1)── ProcessRecord
  │                                  │
  │                                  ├──→ ReceiveBatch (n:1, 每批接收)
  │                                  ├──→ ShipBatch (n:1, 每批发货)
  │                                  └──→ ReturnRecord (n:1, 退件记录)
  │
Factory ◄──(1:n)── User
  │
  └────────────── ProcessRecord.factory_id (外协厂家负责)
```

### 1.2 ProcessRecord 状态字段详解

| 字段 | pending | received | shipped | completed |
|------|---------|----------|---------|-----------|
| total_receive_qty | 0 | ≥0 | ≥0 | ≥0 |
| total_ship_qty | 0 | =0 | >0 | >0 |
| lock_type | none | **entry_lock** | none | none |
| partial_receive | 0 | 0或1 | 0或1 | 0或1 |
| partial_ship | 0 | 0 | 0或1 | 0或1 |
| last_receive_time | NULL | 有值 | 有值 | 有值 |
| last_ship_time | NULL | NULL | 有值 | 有值 |

> **关键发现：** `shipped` 状态时 `lock_type` 变回 `none`，而非保持 entry_lock。
> 这是因为 `ship()` 方法（line 322）设置 `record_status = 'shipped'` 时，
> **没有同时更新 `lock_type`**——lock_type保持原值（通常是none），这是个细节问题。

---

## 二、工序状态机完整分析

### 2.1 状态转换矩阵

```
                    ┌─────────────────────────────────────┐
                    │           本道: pending              │
                    │  _check_lock_for_receive: none ✅    │
                    │  上道ship_qty > 0? (第一道跳过)      │
                    │  receive_qty ≤ 上道可发出量?         │
                    └─────────── receive() ────────────────┘
                                    │ OK
                                    ▼
                    ┌─────────────────────────────────────┐
                    │          本道: received              │  ★ entry_lock
                    │  _check_lock_for_receive: ❌          │  (阻止再次接收)
                    │  _check_lock_for_ship: ❌              │  (阻止发出)
                    └───────── 下道receive() ───────────────┘
                                    │ OK
                                    ▼
                    ┌─────────────────────────────────────┐
                    │  上道: none → relation_lock ★       │
                    │  (下道receive()触发自动升级)         │
                    └───────── ship() ─────────────────────┘
                                    │ OK (无视relation_lock)
                                    ▼
                    ┌─────────────────────────────────────┐
                    │          本道: shipped                │
                    │  lock_type: none (非relation_lock)    │
                    │  可继续发货(partial_ship)             │
                    └─────────────────────────────────────┘
```

### 2.2 各操作前置条件与效果（逐行代码确认）

#### receive() — 接收

**前置条件（line 169-193）：**

| # | 条件 | 代码行 | 错误码 | 错误信息 |
|---|------|--------|--------|----------|
| P1 | `record` 存在 | 169 | RECORD_NOT_FOUND | 流转记录不存在 |
| P2 | `user` 存在 | 170 | USER_NOT_FOUND | 用户不存在 |
| P3 | `user.factory_id == record.factory_id` | 173 | PERMISSION_DENIED | 您不属于该厂家，无权操作 |
| P4 | `lock_type == 'none'` | 176 | RECORD_LOCKED | 记录已锁定（xxx），无法接收 |
| P5 | 上道 `total_ship_qty > 0`（非首道）| 180 | NO_SHIP_FROM_PREV | 上道工序尚无发出量，本道无法接收 |
| P6 | `receive_qty ≤ 上道可发出量 - 本道已收量` | 188-192 | QTY_EXCEED | 接收数量(xxx)超过上道可发出量(xxx) |

**效果（line 221-229）：**

| 字段 | 更新规则 |
|------|----------|
| `total_receive_qty` | `+= receive_qty` |
| `last_receive_time` | `= receive_time` |
| `partial_receive` | `= 1` 当 `batch_no > 1`，否则 `= 0` |
| `record_status` | `pending → received`（仅首次） |
| `lock_type` | `none → entry_lock`（仅首次）|

> ⚠️ **重要细节**：状态更新仅在首次接收时发生（`if record.record_status == "pending"`）。
> 第N次（N≥2）接收只累加数量，**不更新状态，不重置lock**。
> 这与Bug-B1直接相关。

---

#### ship() — 发出

**前置条件（line 277-291）：**

| # | 条件 | 代码行 | 错误码 | 错误信息 |
|---|------|--------|--------|----------|
| S1 | `record` 存在 | 277 | RECORD_NOT_FOUND | - |
| S2 | `user` 存在 | 278 | USER_NOT_FOUND | - |
| S3 | `user.factory_id == record.factory_id` | 281 | PERMISSION_DENIED | 您不属于该厂家，无权操作 |
| S4 | `lock_type ∉ {relation_lock, sync_lock}` | 284 | RECORD_LOCKED | 记录已锁定（xxx），无法发出 |
| S5 | `lock_type != 'entry_lock'` | 134-138 | RECORD_LOCKED | entry_lock状态下，下道未接收确认前无法发出 |
| S6 | `total_receive_qty ≥ ship_qty` | 287 | QTY_EXCEED | 发出数量(xxx)超过累计接收量(xxx) |

**效果（line 318-322）：**

| 字段 | 更新规则 |
|------|----------|
| `total_ship_qty` | `+= ship_qty` |
| `last_ship_time` | `= ship_time` |
| `partial_ship` | `= 1` 当 `batch_no > 1`，否则 `= 0` |
| `record_status` | `→ 'shipped'`（无条件覆盖）|

> ⚠️ **重要细节**：`ship()` 发出后 `lock_type` **保持不变**（不重置为none）。
> 如果原为 `entry_lock`，发出后仍是 `entry_lock`，但 status 变为 `shipped`。
> 由于 `record_status` 已非 `received`，`entry_lock` 对后续操作的影响需重新评估。

---

#### return_goods() — 退件

**前置条件（line 369-401）：**

| # | 条件 | 错误码 | 错误信息 |
|---|------|--------|----------|
| R1 | `user.factory_id == to_record.factory_id`（接收方厂家）| PERMISSION_DENIED | 您不属于该厂家，无权操作 |
| R2 | `return_reason` 非空 | REASON_REQUIRED | 退件原因必填 |
| R3 | `return_qty > 0` | QTY_MUST_POSITIVE | 退件数量必须为正数 |
| R4 | `from_record.lock_type ∉ {relation_lock, sync_lock}` | RECORD_LOCKED | 发送方记录已锁定（xxx），无法退件 |
| R5 | `to_record.lock_type ∉ {relation_lock, sync_lock}` | RECORD_LOCKED | 接收方记录已锁定（xxx），无法退件 |
| R6 | `return_qty ≤ to_record.total_receive_qty` | QTY_EXCEED | 退件数量(xxx)超过接收方已接收量(xxx) |

**效果（line 425-433）：**

| 字段 | 更新规则 |
|------|----------|
| 发送方 `total_ship_qty` | `-= return_qty` |
| 接收方 `total_receive_qty` | `-= return_qty` |
| 发送方 `partial_ship` | `= 1` |
| 接收方 `partial_receive` | `= 1` |
| `record_status` | **不变**（即使数量减至0也不回退）|

> ⚠️ **重要发现**：
> 1. 退件后 `record_status` 不回退，即使 `total_ship_qty` 减至0，状态仍为 `shipped`
> 2. `entry_lock` 的记录可以退件（只阻止 relation_lock/sync_lock）
> 3. 退件后可能导致 `total_ship_qty < 已接收量`，后续发货仍可继续

---

#### upgrade_to_relation_lock() — 升级为关联锁定

**前置条件（line 567-578）：**

| # | 条件 | 错误码 | 错误信息 |
|---|------|--------|----------|
| U1 | `record` 存在 | RECORD_NOT_FOUND | - |
| U2 | 存在下道工序 | NO_NEXT_PROCESS | 没有下道工序，无法升级锁定状态 |
| U3 | 下道 `record_status ∈ {received, shipped, completed}` | NEXT_NOT_RECEIVED | 下道工序尚未接收，无法升级锁定状态 |

**效果：**
- `lock_type → 'relation_lock'`
- 写入 `UPGRADE_LOCK` 类型 `action_log`

> ⚠️ **关键Bug**：此函数**从未在 `receive()` 中被调用**。
> 它只作为独立API端点存在，实际业务流程中从未自动触发。

---

#### upgrade_to_sync_lock() — 升级为同步锁定

**前置条件：**
- `lock_type == 'relation_lock'`（必须先升级关联锁定）
- 需要MOM系统同步触发

> ⚠️ 当前 MOM 系统未接入，此功能未测试。

---

## 三、锁定类型语义深度解析

### 3.1 锁定类型状态矩阵

| lock_type | 含义 | receive() | ship() | return_goods() | 调用场景 |
|-----------|------|:---------:|:------:|:--------------:|----------|
| `none` | 无限制 | ✅ | ✅ | ✅ (from方) | 初始状态 |
| `entry_lock` | 已接收，待本厂完成 | **❌** | **❌** | ✅ (from方) | receive() 首次接收后 |
| `relation_lock` | 下道已确认，本道不可再接收 | ❌ | ✅ | **❌** | upgrade_to_relation_lock() |
| `sync_lock` | 已同步MOM | ❌ | ❌ | **❌** | upgrade_to_sync_lock() |

### 3.2 entry_lock 的真实业务含义

```
本道 receive(qty=100)
  → status: pending → received
  → lock: none → entry_lock
  → 阻止: 本道不能再接收更多（分批接收被Bug-B1阻止）
  → 阻止: 本道不能发货（因为下道还未确认接收）
  → 解锁条件: 下道调用 upgrade_to_relation_lock（本道触发）
  → 解锁后: lock=none，但仍需下道receive()后才能ship
```

### 3.3 relation_lock 的真实业务含义

```
下道 receive() 成功
  → 理想: 调用 upgrade_to_relation_lock(上道)
  → 效果: 上道 lock=none → relation_lock
  → 阻止: 上道不能再接收（防止重复收货）
  → 允许: 上道可以发货（_check_lock_for_ship 不阻止 relation_lock）
  → 最终: 上道 ship() 发出货物
```

**为什么 relation_lock 不阻止 ship()？**
- relation_lock 的语义是"物料已流出确认，下道已收货"
- 本道仍需完成发货动作才能让物料真正流转到下道
- relation_lock 的作用是阻止本道**再接收**（重复收货），而非阻止**发货**

---

## 四、超期判定逻辑（代码确认）

### 4.1 pending 超期

```python
# kanban_service.py: TIMESTAMPDIFF(HOUR, created_at, UTC_TIMESTAMP()) > 48
```

含义：订单创建后（`created_at`），超过48小时仍为 `pending`（未被接收）。

### 4.2 received 超期

```python
# kanban_service.py: TIMESTAMPDIFF(HOUR, last_receive_time, UTC_TIMESTAMP()) > 48
```

含义：接收后（`last_receive_time`），超过48小时仍为 `received`（未发出）。

### 4.3 超期与 lock_type 无关

**重要**：超期判定只看 `record_status`，不看 `lock_type`。
- 一个 `entry_lock` 状态下的 `received` 工序，只要超过48小时未发出，就标记为超期
- 一个 `pending` 超过48小时，即使 `lock_type=none`，也标记为超期

---

## 五、扫码判断逻辑（scan_judge）

### 5.1 二维码格式解析

| 格式 | 解析方式 | 示例 |
|------|----------|------|
| `record_{record_id}` | 直接查找 ProcessRecord | `record_R0101` |
| `process_{process_id}_{factory_id}` | 联合索引查找 | `process_P0101_F002` |

### 5.2 跳转逻辑（代码 line 540-550）

```
record_status == "pending"    → jump_type = "receive"
record_status == "received"  → jump_type = "ship"
record_status == "shipped"   → jump_type = "view"
record_status == "completed" → jump_type = "view"
不存在                       → jump_type = "not_found"
```

**业务逻辑解读：**
- `pending`：扫码后应进入**接收**页面，录入接收数量
- `received`：已到货但未发出，应进入**发出**页面，录入发货数量
- `shipped/completed`：物料已流出，扫码只应**查看**状态

> ⚠️ 注意：`scan_judge` 不检查 `lock_type`，只按 `record_status` 判断。
> 但实际用户体验上，`entry_lock` 状态的 `received` 仍跳转 `ship` 是合理的（需要下道确认后才能发货）。

---

## 六、数量联动规则

### 6.1 核心不变量

```
对于任意工序 i（i > 1）：
  total_receive_qty[i] ≤ total_ship_qty[i-1]

对于任意工序 i：
  total_ship_qty[i] ≤ total_receive_qty[i]

对于完整订单（工序1...n）：
  total_receive_qty[1] ≤ 订单总量
  total_receive_qty[n] = total_ship_qty[n-1] = ... = total_receive_qty[1]（理想状态）
```

### 6.2 数量异常场景

| 场景 | 条件 | 处理方式 |
|------|------|----------|
| 接收量 > 上道发出量 | `receive_qty > available_qty` | 拒绝（P6）|
| 发出量 > 已接收量 | `ship_qty > total_receive_qty` | 拒绝（S6）|
| 退件量 > 接收方已收量 | `return_qty > to.total_receive_qty` | 拒绝（R6）|
| 退件后数量归零 | `total_ship_qty` 减至0 | **允许**，status不回退 |

### 6.3 分批机制

```
第1批接收: batch_no=1, partial_receive=0
第2批接收: batch_no=2, partial_receive=1
第N批接收: batch_no=N, partial_receive=1

# Bug-B1 根因:
#   entry_lock 在首次接收后生效（line 229）
#   _check_lock_for_receive 在 entry_lock 时拒绝（line 121）
#   结果: 第2次 receive 被阻止（即使 entry_lock 允许分批是合理的）
```

---

## 七、权限体系验证

### 7.1 权限检查点

| 操作 | 权限检查 | 代码行 |
|------|----------|--------|
| receive | `user.factory_id == record.factory_id` | line 173 |
| ship | `user.factory_id == record.factory_id` | line 281 |
| return_goods | `user.factory_id == to_record.factory_id`（接收方）| line 374 |
| upgrade_to_relation_lock | **无权限检查** | - |

### 7.2 角色体系

```
enterprise_admin     → 可操作所有工厂
primary_admin        → 可操作 F001
primary_operator     → 可操作 F001
cooperative_admin    → 可操作 F002 或 F003
cooperative_operator → 可操作 F002 或 F003
```

> ⚠️ **Bug-权限漏洞**：代码只检查 `factory_id` 匹配，未检查用户 `role` 是否有权操作该工厂类型。
> 理论上 `cooperative_admin` 不应能操作 `F001`（主厂），但当前代码未阻止。

---

## 八、订单状态联动（严重脱钩）

### 8.1 当前实现

`order_status` 在整个系统中**从未被更新**：
- 仅在订单创建时设为 `pending`
- 之后不受任何 service 层逻辑更新

### 8.2 看板API中的订单状态

```python
# kanban_service.py
# 订单状态直接从DB读取，未重新计算
orders = db.query(Order).filter(...).all()
```

### 8.3 真实状态（vs 代码中的理解）

| 订单 | DB中order_status | 工序实际状态 | 业务含义 |
|------|-----------------|--------------|----------|
| O02 | `pending` | shipped+received | 上道已发出，下道已接收，**但订单状态未更新** |
| O03 | `in_progress` | received+received | 两道均在厂处理中，**但状态提前进入in_progress** |
| O09 | `pending` | shipped+received+pending | 工序流转不完整（第三道pending）|
| O12 | `completed` | completed+completed | ✅ 一致 |

**根本问题**：`order_status` 与工序实际流转状态完全无关，是静态值。

---

## 九、Bug完整清单与根因分析

### B-B1 【🔴 严重】entry_lock 阻止分批接收

**现象**：`received + entry_lock` 状态下无法进行第二次分批接收

**代码证据**：
```python
# line 119-125
def _check_lock_for_receive(self, record: ProcessRecord) -> None:
    if record.lock_type in ["entry_lock", "relation_lock", "sync_lock"]:
        raise RecordServiceError(
            f"记录已锁定（{record.lock_type}），无法接收",
            "RECORD_LOCKED"
        )
```

```python
# line 226-229
if record.record_status == "pending":
    record.record_status = "received"
    record.lock_type = "entry_lock"  # ← 首次接收后 entry_lock
```

**矛盾**：
- 首次接收时 `entry_lock` 是合理的（防止重复接收）
- 但 `entry_lock` **同时阻止了分批接收**（业务需求：同一批次分多次到货）
- 这两个需求互相冲突

**修复建议**：
```python
def _check_lock_for_receive(self, record: ProcessRecord) -> None:
    # entry_lock: 允许分批继续接收（防止重复接收同一批次，而非阻止继续接收）
    # relation_lock: 阻止（物料已流出）
    # sync_lock: 阻止（已同步MOM）
    if record.lock_type in ["relation_lock", "sync_lock"]:
        raise RecordServiceError(...)
```

---

### B-B2 【🔴 严重】relation_lock 未自动升级

**现象**：下道工序 `receive()` 成功后，上道 `lock_type` 未从 `none` 变为 `relation_lock`

**代码证据**：
```python
# receive() 函数（line 157-263）
# 整个函数中从未调用:
#   self.upgrade_to_relation_lock(prev_record.record_id, user_id)
```

```python
# upgrade_to_relation_lock() 函数（line 562-595）
# 函数存在，逻辑正确，但从未被 receive() 调用
```

**影响**：
1. 上道在 `ship()` 时 `lock_type=none`（即使下道已接收）
2. 上道发货后，`lock_type` 仍然是 `none`
3. 上道可能被**重复发货**（因为没有 `relation_lock` 阻止接收）
4. 造成"断链"数据：`received` 状态的下道工序，但上道已 `shipped`

**业务流程中的实际影响**：
- 本质上 `relation_lock` 的作用是防止本道**重复接收**已流出的物料
- 但因为自动升级未实现，本道可能在未确认收到的情况下被重复计入 `total_ship_qty`

**修复建议**：
```python
# 在 receive() 成功执行后（commit 之前），添加：
prev_record = self._get_previous_process_record(record)
if prev_record and prev_record.record_status == "received":
    # 上道已接收 → 升级为 relation_lock（防止本道被重复接收）
    prev_record.lock_type = "relation_lock"
    self._write_action_log(...)
```

---

### B-B3 【🟡 中】订单状态完全脱钩

**现象**：`order_status` 从创建后从未更新，与工序实际流转状态无关

**代码证据**：
- 全系统搜索 `order_status` 更新：无结果
- `kanban_service.py` 直接读 DB 的 `order_status` 而非计算

**影响**：
- 看板/统计中的 `order_status` 不反映真实业务状态
- 企业管理员无法通过订单状态判断订单进展

**修复建议**：
```python
def _update_order_status(self, order_id: str) -> None:
    """根据工序状态更新订单状态"""
    records = self.get_order_records(order_id)
    statuses = [r.record_status for r in records]

    if all(s == "completed" for s in statuses):
        new_status = "completed"
    elif any(s in ("received", "shipped") for s in statuses):
        new_status = "in_progress"
    else:
        new_status = "pending"

    order = self.db.query(Order).filter(Order.order_id == order_id).first()
    if order and order.order_status != new_status:
        order.order_status = new_status
```

此方法应在 `receive()`, `ship()` 等操作成功后被调用。

---

### B-B4 【🟡 中】退件后状态不回退

**现象**：退件导致 `total_ship_qty` 减至0，但 `record_status` 仍为 `shipped`

**代码证据**（line 425-433）：
```python
# 更新双方数量
from_record.total_ship_qty -= return_qty
to_record.total_receive_qty -= return_qty
# partial 标志更新
from_record.partial_ship = 1
to_record.partial_receive = 1
# ⚠️ record_status 完全未处理
```

**影响**：
- `shipped` 状态表示"已发出"，但实际 `total_ship_qty=0`（货物退回）
- 看板中显示已发货，实际货物已退回
- 数量不一致：企业看板上显示数量与实际不符

**修复建议**：在退件数量达到当前 `total_ship_qty` 时，应考虑回退状态：
```python
if from_record.total_ship_qty == 0:
    from_record.record_status = "received"  # 或新增 "returned" 状态
    from_record.lock_type = "entry_lock"
```

---

### B-B5 【🟡 低】权限检查不完整

**现象**：`cooperative_*` 角色理论上不能操作主厂工序，但代码未检查工厂类型

**代码证据**（line 113-117）：
```python
def _check_receive_permission(self, user: User, record: ProcessRecord) -> bool:
    if user.factory_id != record.factory_id:  # 只检查工厂ID相等
        raise RecordServiceError("您不属于该厂家，无权操作", "PERMISSION_DENIED")
    return True
```

**缺失的检查**：
- `cooperative_*` 角色不能操作 `factory_type = 'primary'` 的工厂
- `primary_*` 角色不能操作 `factory_type = 'cooperative'` 的工厂

---

### B-B6 【🟡 低】upgrade_to_relation_lock 无权限校验

**现象**：`upgrade_to_relation_lock` 是公开 API，任何认证用户都可调用

**代码证据**：函数中只有 `record` 存在性检查，无用户-工厂关系校验

---

## 十、完整业务流程场景矩阵

| 场景 | 操作序列 | 关键验证点 | 当前状态 |
|------|----------|-----------|----------|
| **A. 正常流转** | receive(首道) → ship(首道) → receive(二道) → ship(二道) → ... | 每步状态/数量/lock变化 | ⚠️ relation_lock未升级 |
| **B. 分批接收** | receive(30件) → receive(20件) | 第二次被Bug-B1阻止 | ❌ Bug-B1 |
| **C. 分批发货** | receive(100件) → ship(60件) → ship(40件) | 两次partial_ship=1 | ✅ 正常 |
| **D. 退件** | ship(100件) → return_goods(10件) | 数量减少，status不回退 | ⚠️ status不回退是设计选择 |
| **E. 跨工厂操作** | F002操作员 → 操作F003工序 | 权限拒绝 | ✅ 正常 |
| **F. 超期** | pending>48h / received>48h | 超期标记 | ✅ 正常 |
| **G. relation_lock升级** | 下道receive → 上道lock升级 | 自动升级 | ❌ Bug-B2 |
| **H. entry_lock解锁** | entry_lock → 下道receive → 解锁 → ship | 解锁后能发货 | ⚠️ relation_lock未升级导致流程断裂 |
| **I. 完成确认** | 所有工序shipped → completed | completed状态 | ⚠️ 需手动/MOM触发 |
| **J. 扫码judge** | 不同status扫码 | 跳转正确页面 | ✅ 正常 |

---

## 十一、推荐修复优先级

| 优先级 | Bug | 工作量 | 理由 |
|--------|-----|--------|------|
| P0 | B-B2 relation_lock未升级 | 小 | 核心流程断裂，影响数据完整性 |
| P0 | B-B1 entry_lock阻止分批接收 | 小 | 影响日常分批到货业务 |
| P1 | B-B3 订单状态脱钩 | 中 | 看板数据不准确 |
| P2 | B-B4 退件后status不回退 | 小 | 看板数量不一致 |
| P2 | B-B5 权限检查不完整 | 小 | 安全风险 |
| P3 | B-B6 upgrade无权限校验 | 小 | 安全风险 |
