# 外协工序流转追踪系统 — 二次测试验证报告

> 作者：好妹 | 日期：2026-05-23
> 测试方式：API实际调用 + MySQL状态核查
> 测试工具：Python requests + docker exec mysql

---

## 测试环境状态

| 组件 | 状态 |
|------|------|
| MySQL | ✅ 运行中（36条process_records残留） |
| Backend | ✅ http://localhost:8000 |
| Frontend | ✅ http://localhost:8081 |
| 测试账号 | ✅ 5个（SMS验证码正常） |

### 账号映射（实测确认）
| 手机号 | user_id | factory_id | role |
|--------|---------|------------|------|
| 13800138000 | U001 | F001 | enterprise_admin |
| 13800138001 | U002 | F002 | primary_admin |
| 13800138002 | U003 | F002 | primary_operator |
| 13800138003 | ~~U004~~（不存在） | - | - |
| 13800138004 | U005 | F003 | cooperative_operator |

> ⚠️ 原测试文档中 `U004` 为 F003 管理员是**错误**。实际 `13800138003` 对应 `U003`（F002操作员），`U004` 在数据库中不存在。

---

## 测试场景与结果

### 场景A：正常工序流转 + Bug-B1 验证

**数据准备：** O01（3道工序：R0101@F002 → R0102@F003 → R0103@F001）

| # | 操作 | 操作者 | 预期结果 | 实际结果 | 通过 |
|---|------|--------|----------|----------|------|
| A1 | R0101 首次接收 100件 | U002@F002 | ✅ status=received, lock=entry_lock | ✅ status=received, lock=entry_lock | ✅ |
| A2 | R0101 第二次分批接收 50件 | U002@F002 | ❌ entry_lock阻止 | ❌ entry_lock阻止 | ✅ **Bug确认** |

**A1 验证点：**
- 首次接收正确设置 `entry_lock`
- 数量正确累加 `total_receive_qty=100`

**A2 验证点（B-B1核心）：**
- `entry_lock` 状态下第二次接收被拒绝
- 响应：`{"detail": "记录已锁定（entry_lock），无法接收"}`
- DB 状态不变：`received|entry_lock|100|0`

---

### 场景B：entry_lock 发出阻塞 + Bug-B2 验证

| # | 操作 | 操作者 | 预期结果 | 实际结果 | 通过 |
|---|------|--------|----------|----------|------|
| B1 | R0101 在entry_lock下发出 50件 | U002@F002 | ❌ 被entry_lock阻止 | ✅ 被阻止：`entry_lock状态下，下道未接收确认前无法发出` | ✅ |
| B2 | R0102 首次接收（R0101未ship） | U005@F003 | ❌ 上道无发出量 | ✅ 拒绝：`上道工序尚无发出量，本道无法接收` | ✅ |
| B3 | **Bug-B2核心**：R0101 lock是否自动升级relation_lock | - | ❌ 应自动升级但未升级 | ✅ lock仍为entry_lock，未升级 | ✅ **Bug确认** |

**关键发现：**
- R0101 当前状态：`received|entry_lock|100|0`
- B2 无法通过（因为R0101没发出），因此 B3 的验证条件（下道receive触发升级）永远无法满足
- **系统进入死锁状态**

---

### 场景C：手动升级 + relation_lock 不阻止发出

| # | 操作 | 操作者 | 预期结果 | 实际结果 | 通过 |
|---|------|--------|----------|----------|------|
| C1 | 调用 `upgrade_to_relation_lock` API | U002@F002 | lock升级为relation_lock | ❌ **API路由不存在**（404） | ❌ |
| C2 | R0101 在entry_lock下发出 | U002@F002 | ❌ 仍被阻止 | ✅ 被阻止 | ✅ |
| C3 | R0102 接收 | U005@F003 | ❌ R0101未ship，无法接收 | ✅ 拒绝 | ✅ |

**C1 关键发现：**
- `upgrade_to_relation_lock()` 函数在 `record_service.py` 中存在
- 但**完全没有 API 路由**暴露它
- 调用 `/records/lock/upgrade` 返回 404 Not Found
- 该函数只能通过代码内部调用，业务流程中从未触发

---

### 场景D：分批接收二次确认

| # | 操作 | 操作者 | 预期 | 实际 | 通过 |
|---|------|--------|------|------|------|
| D1 | R0201 首次接收 120件 | U002@F002 | ✅ | ✅ received\|entry_lock\|120\|0 | ✅ |
| D2 | R0201 第二次分批接收 80件 | U002@F002 | ❌ 被entry_lock阻止 | ❌ 被阻止：`记录已锁定（entry_lock）` | ✅ **Bug再次确认** |

---

### 场景E：apply_unlock 解锁测试

| # | 操作 | 操作者 | 预期结果 | 实际结果 | 通过 |
|---|------|--------|----------|----------|------|
| E1 | 调用 `apply_unlock` API | U002@F002 | lock_type 变为 none | ❌ **lock_type 不变**（仍是entry_lock） | ❌ |
| E2 | 解锁后 R0101 分批接收 50件 | U002@F002 | ✅ 接收成功 | ❌ 仍被阻止 | ❌ |

**🔴 重大发现：apply_unlock 是桩函数（stub）**

代码证据（records.py line 251-252）：
```python
# 实际应该创建审批请求，这里简化处理
return RecordResponse.model_validate(record)  # 直接返回，未修改任何字段！
```

`apply_unlock` **完全不修改 lock_type**，只是读取并返回原记录。

---

### 场景F：超期检测 + 看板数据

| # | 操作 | 操作者 | 预期结果 | 实际结果 | 通过 |
|---|------|--------|----------|----------|------|
| F1 | 看板统计 | U002@F002 | 返回超期工序数量 | 返回 total=0 pending=0（数据为空） | ⚠️ 数据问题 |
| F2 | 订单列表 | U002@F002 | 返回各订单状态 | 返回 status=None（数据映射问题） | ⚠️ |

---

## Bug 确认汇总

### 🔴 P0 — entry_lock 死锁（系统级设计缺陷）

**描述：** `entry_lock` 设计的解锁条件（下道接收）在当前流程中**永远无法满足**，且 `apply_unlock` 和 `upgrade_to_relation_lock` 均无法打破死锁。

**触发路径：**
```
receive(首道) → status=received, lock=entry_lock
    ↓
entry_lock 阻止 ship（需要下道接收才能解锁）
    ↓
下道无法 receive（因为上道从未 ship）
    ↓
→ 死锁
```

**三层解锁机制全部失效：**

| 解锁方式 | 代码状态 | 有效性 |
|----------|----------|--------|
| `receive()` 中自动调用 `upgrade_to_relation_lock()` | **从未调用** | ❌ |
| `apply_unlock` API | **桩函数，不修改任何字段** | ❌ |
| `upgrade_to_relation_lock` API路由 | **API路由不存在** | ❌ |

**根本原因：** 代码中存在升级函数，但：
1. `receive()` 中从未调用
2. `upgrade_to_relation_lock` 没有暴露 API
3. `apply_unlock` 是桩函数，不做任何修改

---

### 🟡 P1 — Bug-B1 entry_lock 阻止分批接收

**描述：** `entry_lock` 状态下无法进行第二次分批接收

**代码证据：**
```python
# line 119-125
def _check_lock_for_receive(self, record: ProcessRecord) -> None:
    if record.lock_type in ["entry_lock", "relation_lock", "sync_lock"]:
        raise RecordServiceError(...)  # entry_lock 被阻止
```

```python
# line 226-229
if record.record_status == "pending":
    record.record_status = "received"
    record.lock_type = "entry_lock"  # 首次接收后 entry_lock
    # 之后第N次接收不会重入这个if，但 _check_lock 已阻止
```

**影响：** 分批到货场景（如供应商分批发货）无法正常录入。

---

### 🟡 P1 — Bug-B3 order_status 完全脱钩

**描述：** `order_status` 从创建后从未更新，与工序实际流转状态无关。

**代码证据：** 全系统搜索 `order_status` 更新：无结果。`kanban_service.py` 直接读 DB 原始值。

**影响：** 看板/统计中的订单状态不反映真实业务进展。

---

## 修复建议（按优先级）

### P0 — 修复 entry_lock 死锁

**方案A（推荐）：在 receive() 中自动升级上道 lock**

```python
# 在 record_service.py receive() 函数末尾，commit 之前添加：
prev_record = self._get_previous_process_record(record)
if prev_record and prev_record.record_status == "received":
    prev_record.lock_type = "relation_lock"
    self._write_action_log(
        user_id=user_id,
        action_type="UPGRADE_LOCK",
        target_table="process_records",
        target_id=prev_record.record_id,
        old_value={"lock_type": "none"},
        new_value={"lock_type": "relation_lock"}
    )
```

**方案B：修复 apply_unlock 桩函数**

将 `apply_unlock` 改为真正解除 entry_lock：

```python
# records.py line 251-252 改为：
record.lock_type = "none"
self._write_action_log(...)
self.db.commit()
self.db.refresh(record)
return RecordResponse.model_validate(record)
```

### P1 — 修复 Bug-B1

```python
def _check_lock_for_receive(self, record: ProcessRecord) -> None:
    # entry_lock 不阻止分批接收（已在本厂接收，可继续分批到货）
    # relation_lock/sync_lock 阻止
    if record.lock_type in ["relation_lock", "sync_lock"]:
        raise RecordServiceError(...)
```

### P1 — 修复 order_status 联动

在 `receive()`, `ship()` 等操作成功后调用状态更新：

```python
def _update_order_status(self, order_id: str) -> None:
    records = self.get_order_records(order_id)
    statuses = [r.record_status for r in records]
    if all(s == "completed" for s in statuses):
        new_status = "completed"
    elif any(s in ("received", "shipped") for s in statuses):
        new_status = "in_progress"
    else:
        new_status = "pending"
    order = self.db.query(Order).filter(Order.order_id == order_id).first()
    if order and order.order_status != new_status:
        order.order_status = new_status
```

---

## 测试结论

| 验证项 | 结果 | 说明 |
|--------|------|------|
| 首次接收正常 | ✅ | entry_lock 正确设置 |
| entry_lock 阻止分批接收 | ✅ Bug确认 | Bug-B1 |
| entry_lock 阻止发出 | ✅ | 符合设计 |
| relation_lock 未自动升级 | ✅ Bug确认 | Bug-B2（函数存在但从未调用）|
| upgrade API 不存在 | ✅ Bug确认 | 404 |
| apply_unlock 是桩函数 | ✅ Bug确认 | 不修改任何字段 |
| 订单状态联动 | ❌ 完全脱钩 | Bug-B3 |
| 看板统计 | ⚠️ 数据问题 | 需重建测试数据 |

**系统当前状态：核心流程存在 P0 死锁Bug，无法完成正常的工序流转。**
