# MADM-Solo 主流程验收报告 — 外协制件工序流转系统

日期：2026-06-10  
项目路径：`/home/takemehome/outsourcing-track`  
验收方式：真实 API + 真实 MySQL DB 核对 + 自动测试 + Docker 构建

## 结论

✅ **当前主流程切片可交付。**

已跑通：登录/鉴权、看板、首道接收、分批接收、首道发出、下道接收、退件、数据库状态核对。  
质量门结果：后端测试通过、前端 Docker 构建通过、后端服务健康、主流程验收脚本通过。

## 质量门结果

| 检查项 | 结果 | 证据 |
|---|---:|---|
| Backend health | ✅ 通过 | `GET /health -> 200` |
| Backend pytest | ✅ 通过 | `13 passed, 11 warnings` |
| 主流程 API+DB 验收 | ✅ 通过 | `7/7 passed` |
| Frontend Docker build | ✅ 通过 | `docker compose build frontend -> 0` |
| Docker 服务状态 | ✅ 通过 | backend up；frontend up；mysql healthy |

## 主流程验收明细

| 场景 | 结果 | 实际 |
|---|---:|---|
| Backend health | ✅ | `200` |
| 看板订单接口 | ✅ | `200` |
| R1 首次接收 API | ✅ | `200` |
| R1 分批接收 API | ✅ | `200` |
| R1 发出 API | ✅ | `200` |
| R2 下道接收 API | ✅ | `200` |
| R2 退件回 R1 API | ✅ | `200` |


## 最终数据库状态快照

```text
MADM_R1 | 1 | shipped | entry_lock | 100 | 90 | 1 | 1
MADM_R2 | 2 | received | entry_lock | 90 | 0 | 1 | 0
MADM_R3 | 3 | pending | none | 0 | 0 | 0 | 0
in_progress
3
1
```

含义摘要：
- `MADM_R1`：已接收 100，已发出后退件调整为 90，状态 `shipped`。
- `MADM_R2`：下道已接收，退件后接收量为 90，状态 `received`。
- `MADM_R3`：未进入下一道，保持 `pending`。
- 订单状态：`in_progress`。
- 接收批次数：3；发出批次数：1。

## 本轮修复内容

1. 修复后端自动测试阻断项：
   - `Notification` / `NotificationType` 模型导入缺失。
   - SQLite 内存测试库连接隔离导致 `no such table`。
   - `SMSCode.id` 在 SQLite 下 `BigInteger autoincrement` 不兼容。
   - 测试 fixture 与当前 ORM 字段不一致：`order_status`、`primary_factory_id`、`total_qty`、`process_seq` 等。
   - 看板测试缺少鉴权 header。
   - 通知测试数据与真实权限/工序约束不一致。
2. 已将主流程验收脚本结果保存到：
   - `docs/acceptance/main-flow-validation-result.json`
3. 已更新复用经验到 skill：
   - `project-claude-code-collaboration` 增加 Pytest + SQLAlchemy SQLite 测试库陷阱。

## 非阻断问题

- `pytest_asyncio` 配置警告：不影响当前测试通过，后续可显式设置 loop scope。
- Pydantic V2 deprecation warnings：不影响当前运行，后续可迁移到 `ConfigDict`。
- Git 当前存在大量未入库文件/历史未整理变更；正式发布前建议单独整理 commit。

## 交付判定

✅ **通过验收，可进入下一切片：H5扫码录入/前端主流程联调/飞书多维表格对接。**
