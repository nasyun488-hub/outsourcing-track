# SPEC P0 手机摄像头扫码现场验收支撑开发测试报告

生成时间：2026-06-17 00:30

## 结论

已按 `SPEC.MD` 后续优先级先完成 P0：真实手机摄像头扫码现场验收支撑。

注意：本轮完成的是“具备真机现场验收条件”，不是宣称真实手机摄像头全路径已经通过；真实手机扫码仍需按现场清单执行。

## 本轮完成内容

1. HTTPS 前端现场验收入口
   - 新增 `frontend/nginx.https.conf`
   - 新增 `docker-compose.https.yml`
   - HTTPS 访问：`https://<局域网IP>:8443/scan`
   - 已验证本机 HTTPS `/scan` 返回 200
   - 已验证响应头包含 `Permissions-Policy: camera=(self)`

2. 手机扫码页安全上下文提示
   - 修改 `frontend/src/views/ScanPage.vue`
   - 增加 HTTPS / localhost 安全上下文提醒
   - 增加“真机扫码验收”操作提示
   - 相机启动失败提示覆盖：HTTPS、浏览器权限、摄像头占用

3. 现场扫码样张
   - 新增 `scripts/generate_mobile_scan_qr_samples.mjs`
   - 生成目录：`docs/acceptance/mobile-scan-samples/`
   - 样张：
     - `record_DEMO_PENDING_R1.svg`
     - `record_DEMO_RECEIVED_R1.svg`
     - `bad_code_for_field_test.svg`

4. 现场验收清单
   - 新增 `docs/acceptance/mobile-camera-field-test-checklist-2026-06-16.md`
   - 覆盖手机 HTTPS 访问、摄像头权限、样张扫码、API/DB 回查、控制台无 JS error

5. TDD/回归脚本补强
   - 新增 `scripts/validate_mobile_camera_p0_readiness.py`
   - 新增 `scripts/validate_local_http_no_proxy_contract.py`
   - 新增 `scripts/validate_https_secret_hygiene.py`
   - 修复 `validate_main_flow.py`、`human_ui_traversal.py`：本地接口请求绕过运行环境代理，避免 localhost 被代理返回 502 的误判
   - `.gitignore` 增加 `certs/`、`*.key`、`*.pem`，避免提交本地 HTTPS 私钥

## 验证结果

| 验证项 | 结果 |
|---|---|
| 后端 `/health` | 200 |
| HTTP 前端 `/` | 200 |
| HTTPS 前端 `/scan` | 200 |
| HTTPS Camera Policy | `Permissions-Policy: camera=(self)` |
| P0 手机扫码支撑契约 | 通过 |
| 本地 HTTP no-proxy 契约 | 通过 |
| HTTPS 证书密钥不入库契约 | 通过 |
| 扫码购物车 UI 契约 | 通过 |
| H5 体验契约 | 通过，checked_pages=11 |
| API 辅助移动端遍历 | 65/65 通过 |
| 主流程 API + DB 验证 | 通过 |
| 前端 Vite 构建 | 通过 |
| Docker HTTP 前端镜像构建 | 通过 |
| Docker HTTPS 前端镜像构建 | 通过 |
| 浏览器 DOM 抽测 `/scan` 手机扫码 Tab | 通过，控制台无 JS error |

## 已部署/运行状态

- MySQL：运行中，healthy
- Backend：运行中，`http://127.0.0.1:8000/health` = 200
- HTTP Frontend：运行中，`http://127.0.0.1:8081/` = 200
- HTTPS Frontend：运行中，`https://127.0.0.1:8443/scan` = 200（本地自签证书，仅用于现场验收测试）

## 明确边界

已完成：后端/API/DB 主流程回归、H5 扫码页面提示与 HTTPS 验收入口、样张、现场验收清单、自动化契约测试、Docker 构建与本机 HTTPS smoke。

未完成：真实手机摄像头在具体手机型号/浏览器/局域网环境下的像素级扫码全路径验收；该项需按清单人工执行并补记录。

## 关键附件

- 现场验收清单：`docs/acceptance/mobile-camera-field-test-checklist-2026-06-16.md`
- 扫码样张目录：`docs/acceptance/mobile-scan-samples/`
- 主流程结果：`docs/acceptance/main-flow-validation-result.json`
- UI 遍历结果：`docs/acceptance/human-ui-traversal-result.json`
