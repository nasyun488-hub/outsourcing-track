# 外协工序流转追踪系统 — 继续开发 40+ 样本多角色复测报告

生成时间：2026-06-16  
项目路径：`/home/takemehome/outsourcing-track`

## 结论

本轮继续开发测试已完成：**后端/API/DB 主流程通过，40+ 样本数据通过，多角色 H5/API 辅助遍历通过，前端关键页面已部署并可访问。**

需要明确边界：本轮做了浏览器 DOM 实测与 API 辅助移动端遍历，确认前端已有扫码枪优先、相机扫码入口、看板详情数量展示等体感改造；但未做完整真机摄像头扫码的像素级全路径验收。

## 本轮修复/补强

1. 修复 `scripts/validate_main_flow.py` 的 DB 回查方式：
   - 改为 `docker exec -i ... mysql --default-character-set=utf8mb4`。
   - SQL 通过 stdin 输入，避免 shell 转义问题。
   - 使用容器内 `$MYSQL_ROOT_PASSWORD`，不在脚本中硬编码/输出数据库密码。
   - 修复后中文字段不再出现脚本回查乱码。

2. 修正 `scripts/human_ui_traversal.py` 过期判断：
   - 扫码页已接入 `@zxing/browser`，相机扫码入口不再标记为未完成。
   - 通知“全部已读”已真实调用 `/notifications/read-all`，前端 store 已对齐。
   - 导出 Excel 改为直接二进制校验，避免通用 JSON 请求函数导致超时/误判。

3. 完成前端构建与部署：
   - `npm run build` 通过。
   - `docker compose build frontend` 通过。
   - `docker compose up -d --no-deps frontend` 后前端容器运行正常。

## 系统状态

| 项目 | 结果 |
|---|---:|
| Backend `/health` | 200 |
| Frontend `/` | 200 |
| MySQL 容器 | healthy |
| 前端构建 | 通过 |
| Python 编译检查 | 通过 |

## 40+ 样本数据验证

脚本：`scripts/extend_demo_data_40_plus.py`  
结果文件：`docs/acceptance/demo-data-40-plus-result.json`

| 指标 | 结果 |
|---|---:|
| 总订单数 | 49 |
| 批量新增订单 | 42 |
| 流转记录数 | 147 |
| 参与厂家数 | 4 |
| pending 订单 | 7 |
| in_progress 订单 | 36 |
| completed 订单 | 6 |
| 每订单缺 3 工序异常 | 0 |
| 下道接收超过上道发出异常 | 0 |
| 发出超过接收异常 | 0 |
| 通知乱码异常 | 0 |

角色覆盖：`enterprise_admin`、`primary_admin`、`a_factory_operator`、`b_factory_operator`、`surface_operator`、`final_process_operator`。

## 主流程 API + DB 交叉验证

脚本：`scripts/validate_main_flow.py`  
结果文件：`docs/acceptance/main-flow-validation-result.json`

| 验证项 | 结果 |
|---|---|
| Backend health | ✅ 通过 |
| 看板订单接口 | ✅ 通过 |
| R1 首次接收 API | ✅ 通过 |
| R1 分批接收 API | ✅ 通过 |
| R1 发出 API | ✅ 通过 |
| R2 下道接收 API | ✅ 通过 |
| R2 退件回 R1 API | ✅ 通过 |
| DB 最终回查 | ✅ 通过 |

最终 DB 状态摘要：

```text
MADM_R1：shipped，receive=100，ship=100
MADM_R2：received，receive=100，ship=0
MADM_R3：pending，receive=0，ship=0
订单状态：in_progress
接收批次：3
发出批次：1
```

## H5/API 辅助移动端遍历

脚本：`scripts/human_ui_traversal.py`  
结果文件：`docs/acceptance/human-ui-traversal-result.json`  
报告文件：`docs/acceptance/human-ui-traversal-report.md`

| 指标 | 结果 |
|---|---:|
| 总检查项 | 66 |
| 通过 | 66 |
| 失败 | 0 |

覆盖页面/路径：

- 首页 `/`
- 登录 `/login`
- 扫码 `/scan`
- 看板 `/kanban`
- 工序详情 `/kanban/:order_id`
- 接收 `/receive/:record_id`
- 发出 `/ship/:record_id`
- 详情 `/view/:record_id`
- 通知 `/notifications`
- 导出 `/export`
- 管理页 `/admin/users`、`/admin/factories`

## 浏览器 DOM 实测

本轮用浏览器直接访问并登录前端：

1. `/scan`
   - 页面显示“扫码枪优先录入”。
   - 扫码枪、批量、相机 3 个入口可见。
   - 相机 Tab 可切换，显示“打开相机预览”。
   - 控制台无 JS error。

2. `/kanban/DEMO_PENDING_001`
   - 页面显示 3 道工序。
   - 首道“首道精加工”显示：可接收 80、可发出 0、累计接收 0、累计发出 0。
   - 二道/末道等待上道发出，按钮禁用原因明确。
   - 控制台无 JS error。

截图证据：`/home/takemehome/.hermes/cache/screenshots/browser_screenshot_710a7950f59b4b638549284a3ea574af.png`

## DB 回查汇总

```text
orders	49
records	147
pending	7
in_progress	36
completed	6
bad_ship_gt_receive	0
bad_downstream_receive	0
mojibake_notifications	0
```

## API 样本回查

管理员看板接口样本：

- `/api/kanban/orders`：返回 20 条分页订单。
- `/api/kanban/orders/DEMO_PENDING_001/processes`：返回 3 道工序。
- 首道工序样本：
  - process_name：首道精加工
  - available_receive_qty：80
  - available_ship_qty：0
  - current_receive_qty：0
  - current_ship_qty：0
  - prev_ship_qty：80

## 修改文件摘要

本轮实际触达/修改：

- `scripts/validate_main_flow.py`
- `scripts/human_ui_traversal.py`
- `docs/acceptance/demo-data-40-plus-result.json`
- `docs/acceptance/main-flow-validation-result.json`
- `docs/acceptance/human-ui-traversal-result.json`
- `docs/acceptance/human-ui-traversal-report.md`
- `docs/acceptance/继续开发40样本多角色复测报告-2026-06-16.md`

另：当前仓库仍显示大量未跟踪目录（`backend/ frontend/ docs/ scripts/`）和 `docker-compose.yml` 未提交变更；提交前需要单独整理 Git 基线，避免把历史未跟踪文件状态误判成本轮新增。

## 最终判断

- **后端/API/DB 主流程：通过。**
- **40+ 样本数据逻辑：通过。**
- **多角色权限/API 辅助遍历：通过。**
- **前端关键页面体验：已明显改造并可运行；扫码枪/批量/相机入口、看板详情、通知、导出等路径通过本轮验证。**
- **真机摄像头扫码：未做实机验收，仍需后续拿手机/扫码枪做现场验证。**
