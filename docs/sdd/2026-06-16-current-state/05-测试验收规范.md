# 测试验收规范

> 项目：外协工序流转追踪系统  
> 生成日期：2026-06-16  
> 当前基线：`9cc5f7d` / `baseline-h5-scan-2026-06-16`  
> 当前状态：后端/API/DB 主流程通过；H5 全页面体验优化与扫码购物车式录入已完成并部署；真实手机摄像头实拍扫码未做最终像素级闭环。

## 1. 验收原则

采用“业务逻辑 + API + DB + H5 页面体验”交叉验证，不以单个接口 200 作为完成标准。

## 2. 当前已通过验证

| 验证项 | 命令/方式 | 最近状态 |
|---|---|---|
| Python 编译 | `python3 -m compileall -q backend/app scripts` | ✅ 通过 |
| 前端构建 | `cd frontend && npm run build` | ✅ 通过 |
| Docker 前端构建 | `docker compose build frontend` | ✅ 通过 |
| 健康检查 | `curl /health`、`curl frontend /` | ✅ backend=200、frontend=200 |
| 主流程 API+DB | `python3 scripts/validate_main_flow.py` | ✅ 通过 |
| 40+ 样本 | `python3 scripts/extend_demo_data_40_plus.py` | ✅ 通过 |
| H5 辅助遍历 | `python3 scripts/human_ui_traversal.py` | ✅ 通过 |
| 扫码契约 | `python3 scripts/validate_scan_cart_ui.py` | ✅ 通过 |
| 全页面体验契约 | `python3 scripts/validate_h5_experience_upgrade.py` | ✅ 通过 |
| 浏览器抽测 | 登录、看板、详情、扫码 | ✅ 无 JS error |

## 3. 标准回归命令

```bash
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:$PATH
cd /home/takemehome/outsourcing-track
python3 -m compileall -q backend/app scripts
python3 scripts/extend_demo_data_40_plus.py
python3 scripts/validate_main_flow.py
python3 scripts/human_ui_traversal.py
python3 scripts/validate_scan_cart_ui.py
python3 scripts/validate_h5_experience_upgrade.py
(cd frontend && npm run build)
docker compose build frontend
docker compose up -d --no-deps frontend
curl -s -o /dev/null -w 'backend=%{http_code}
' http://localhost:8000/health
curl -s -o /dev/null -w 'frontend=%{http_code}
' http://localhost:8081/
```

## 4. H5 体验验收矩阵

| 页面 | 必查体验点 |
|---|---|
| 登录 | 业务定位清晰、错误提示人话、验证码流程可用 |
| 首页 | 今日工作台，不只是菜单 |
| 看板 | 外协流转雷达、当前卡点、风险/筛选可见 |
| 详情 | 订单作战室、工序时间线、下一步建议 |
| 扫码 | 连续扫码、清单、默认数量、改数量、删记录、异常区、统一提交 |
| 接收/发出 | 可接收/可发出数量突出，一键全部，超量禁止 |
| 通知 | 异常队列/待办，已读与跳转处理 |
| 管理 | 人员/厂家审核、权限说明、状态处理 |
| 导出 | 时间/厂家/订单过滤、Blob 下载、导出说明 |

## 5. 验收报告位置

| 文件 | 说明 |
|---|---|
| `docs/acceptance/main-flow-validation-result.json` | 主流程 JSON 结果 |
| `docs/acceptance/human-ui-traversal-result.json` | H5 辅助遍历 JSON 结果 |
| `docs/acceptance/全页面H5体验优化开发验收报告-2026-06-16.md` | 全页面体验验收报告 |
| `docs/acceptance/扫码录入超市购物式优化二次落地验收报告-2026-06-16.md` | 扫码二次落地验收报告 |
| `docs/acceptance/继续开发40样本多角色复测报告-2026-06-16.md` | 40+ 样本与多角色复测 |

## 6. 未完成验收边界

| 边界 | 当前状态 | 需要补充 |
|---|---|---|
| 真机摄像头扫码 | 未最终覆盖 | HTTPS 环境 + 手机权限 + 真实码样张 + DB 回查 |
| 外部系统正式对接 | 部分覆盖 | 已覆盖 MOM/标准文件导入；真实 MOM 接口联调与大批量压测待补充；飞书接入已取消 |
| 多浏览器兼容 | 部分覆盖 | iOS Safari、安卓微信/浏览器专项测试 |
| 大批量性能 | 未专项压测 | 生产规模订单/记录响应测试 |
